apiVersion: v1
kind: Service
metadata:
  name: {{ include "waterflow.fullname" . }}
  labels:
    {{- include "waterflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "waterflow.selectorLabels" . | nindent 4 }}