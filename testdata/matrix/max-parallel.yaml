name: matrix-max-parallel
on: push

jobs:
  test:
    runs-on: linux-amd64
    strategy:
      matrix:
        version: [1.20, 1.21, 1.22, 1.23, 1.24]
      max-parallel: 2
    steps:
      - name: Test
        uses: test@v1
        with:
          version: ${{ matrix.version }}
